---
interface Props {
  className?: string;
}

/* <!-- Badge Template Astro --> */
const { className } = Astro.props;
---
<span class={` inline-flex flex-row gap-1 justify-center items-center text-sm font-medium px-2 py-0.5 rounded-md mb-2 w-fit ${className}`}>
  <slot></slot>
</span>
